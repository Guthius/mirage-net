<Window x:Class="Mirage.Windows.WIndowTraining"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Training"
        Height="140" Width="280">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="What stat would you like to train?" Margin="0,0,0,10" />

        <ComboBox Grid.Row="1" x:Name="cmbStat" VerticalAlignment="Top" SelectedIndex="0">
            <ComboBoxItem Content="Strength" />
            <ComboBoxItem Content="Defense" />
            <ComboBoxItem Content="Magic" />
            <ComboBoxItem Content="Speed" />
        </ComboBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5,0,0">
            <Button Padding="3" Width="80" Content="Train" Margin="0,0,5,0" Click="OnAccept" />
            <Button Padding="3" Width="80" Content="Cancel" Click="OnCancel" />
        </StackPanel>
    </Grid>
</Window>